# Override file for Easypanel - Git Clone Solution
# This approach clones the repo directly in the container
# No file mounting required - completely self-contained

services:
  # No overrides needed - git clone handles everything