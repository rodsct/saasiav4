# Override file for Easypanel - NO BUILD REQUIRED
# This configuration uses base Node image + volumes instead of Dockerfile

services:
  app:
    # No build section needed - using base image with runtime installation